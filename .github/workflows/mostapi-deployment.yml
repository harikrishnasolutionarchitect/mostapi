name: hello-world
on: 
  push:
    tags:
      - v0.1.0

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: uptime
        run: uptime
  my-job-api:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: uptime
        run: uptime